"""Kirk communication-channel plugins for the two backends.

Discovered by kirk_shim.py before kirk parses --com; both channels
present the same guest contract from _common.py, so kirk sees one
target shape whichever backend runs.
"""
