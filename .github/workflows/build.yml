# Build elfuse and run the HVF runtime tests.
#
#   build-macos     : compile + entitlement check on macOS Apple Silicon
#   oci-linux       : elfuse-oci unit tests on Linux (pure Go, no HVF)
#   oci-image-macos : elfuse-oci unit tests plus the real hdiutil round trip
#   runtime-macos   : HVF runtime tests on self-hosted Apple Silicon,
#                     including release, ASAN, UBSAN, and TSAN variants;
#                     the release leg also boots guests through elfuse-oci
#   workload        : one real workload image per leg under elfuse-oci
#
# Runtime and sanitizer tests require Hypervisor.framework, which
# GitHub-hosted macOS runners do not expose. Those tests run on self-hosted
# Apple Silicon runners; the hosted job stops at build.
name: Build

# paths-ignore skips the run only when EVERY changed file matches, so the list
# below is what cannot reach a build or a test: prose, editor and agent
# configuration, the Frama-C stubs (reached only through mk/verify.mk, never
# through -Isrc), and the other workflows. Everything else, src/ and tests/ and
# mk/ and scripts/ and this file included, still runs the full job. Adding a
# path here says "no build or test input can depend on this"; anything new is
# unlisted and therefore builds, which is the safe direction. .gitignore is NOT
# listed for that reason: checkout's git clean -ffdx wipes exactly what it
# names, which is why the fixture-restore step further down exists at all.
#
# On pull_request only. Actions does not expand YAML anchors, so a filter on
# push too would be a second copy with nothing keeping the two in step, and a
# drift between them means a branch and its own merge commit run different
# checks. The cost of dropping it is one build on a doc-only merge to main.
# verify.yml keeps its push filter instead, and the asymmetry is the price:
# the same doc-only merge would start seventeen macOS proof legs there.
#
# The failure directions differ from the proof scoping in proof-scope.py, which
# is why that one needs a self-test and this one does not: a stale entry here
# (path renamed) matches nothing and the workflow runs MORE, while a stale
# entry in HARNESS_FILES widens nothing and the proofs run LESS.
#
# One condition on all of that: main carries no branch protection and no
# ruleset today, so a run that never starts blocks nothing. A path filter skips
# the whole run rather than reporting a skipped job, and a REQUIRED check that
# never reports stays pending forever. Whoever makes these checks required has
# to move the filtering into the jobs (a cheap classifier job plus "if:" on the
# expensive ones) at the same time.
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'LICENSE'
      - '.agents/**'
      - '.claude/**'
      - '.editorconfig'
      - '.clang-format'
      - 'frama-c-stubs/**'
      - '.github/workflows/lint.yml'
      - '.github/workflows/static-analysis.yml'
      - '.github/workflows/verify.yml'
  workflow_dispatch:
  # A merge queue runs the merged result, not the PR, and merge_group carries
  # no path filter and no pull_request payload: every check runs in full there.
  # Without this trigger a queue would merge with nothing having run at all.
  merge_group:

# Cancel in-progress runs for the same PR; keep main runs going.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

permissions:
  contents: read

jobs:
  # Build verification on macOS Apple Silicon (no HVF runtime tests).
  # Hosted runners don't expose Hypervisor.framework, so this job stops at
  # `make elfuse` + entitlement check.
  build-macos:
    name: Build (macOS Apple Silicon)
    runs-on: macos-15
    timeout-minutes: 15
    env:
      GNU_OBJCOPY: /opt/homebrew/opt/binutils/bin/objcopy
      HOMEBREW_NO_INSTALL_CLEANUP: 1
      HOMEBREW_NO_AUTO_UPDATE: 1
      BREW_PKGS: binutils
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Cache Homebrew downloads
        # No restore-keys: a partial match would mask upstream regressions.
        uses: actions/cache@v6
        with:
          path: ~/Library/Caches/Homebrew/downloads
          key: brew-${{ runner.os }}-${{ runner.arch }}-${{ env.BREW_PKGS }}

      - name: Confirm host is arm64
        run: |
          set -euo pipefail
          uname -mrs
          test "$(uname -m)" = "arm64"

      - name: Install GNU objcopy
        # shellcheck disable=SC2086 -- BREW_PKGS is a space-separated list.
        run: |
          set -euo pipefail
          brew install --quiet $BREW_PKGS
          "$GNU_OBJCOPY" --version | head -1

      - name: Build elfuse
        run: |
          set -euo pipefail
          clang --version | head -1
          make elfuse

      - name: Verify HVF entitlement is embedded
        run: .ci/hvf-verify-elfuse-binary.sh build/elfuse

      - name: Upload elfuse binary
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v7
        with:
          name: elfuse-${{ runner.os }}-${{ runner.arch }}
          path: build/elfuse
          retention-days: 7
          if-no-files-found: warn

  # The Go CLI is pure Go: no HVF entitlement, no codesigning, so the
  # cheapest runner covers vet and the race-detected test suite.
  oci-linux:
    name: elfuse-oci (Linux)
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Lint and test elfuse-oci
        run: |
          set -euo pipefail
          go version
          make oci-lint
          scripts/ci/oci-lib-selftest.sh
          make oci-test

  # Hosted macOS gives hdiutil and case-sensitive APFS but no HVF: this
  # is the only place the race detector sees the darwin sparsebundle and
  # clone code, and the only hosted runner that can run the real hdiutil
  # round-trip.
  oci-image-macos:
    name: OCI image CLI (macOS Apple Silicon)
    runs-on: macos-15
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Test elfuse-oci on darwin
        run: |
          set -euo pipefail
          go version
          make oci-lint
          make oci-test

      - name: Real hdiutil sparsebundle round-trip
        run: make oci-test-hdiutil

  runtime-macos:
    name: Runtime (${{ matrix.name }})
    needs: build-macos
    if: >
      github.repository == 'sysprog21/elfuse' &&
      (github.event_name == 'push' || github.event_name == 'pull_request' ||
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'merge_group')
    runs-on: [self-hosted, macOS, arm64]
    # Sanitizer builds run several times slower than the release build, so the
    # job budget and the per-test TEST_TIMEOUT are set per leg. Without
    # that, a TSAN-slowed guest overruns both, surfacing as TIMEOUT reds
    # indistinguishable from a real hang.
    timeout-minutes: ${{ matrix.job_timeout }}
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        include:
          - name: Release
            sanitizer: release
            extra_cflags: ''
            asan_options: ''
            ubsan_options: ''
            tsan_options: ''
            test_timeout: ''
            job_timeout: 30
            run_matrix: true
            check_target: check
            brew_pkgs: binutils qemu
          - name: ASAN
            sanitizer: asan
            extra_cflags: -O1 -g -fsanitize=address -fno-omit-frame-pointer
            asan_options: abort_on_error=1:detect_leaks=0
            ubsan_options: ''
            tsan_options: ''
            test_timeout: '30'
            job_timeout: 30
            run_matrix: false
            check_target: check-sanitizer
            brew_pkgs: binutils
          - name: UBSAN
            sanitizer: ubsan
            extra_cflags: -O1 -g -fsanitize=undefined -fno-sanitize-recover=undefined -fno-omit-frame-pointer
            asan_options: ''
            ubsan_options: halt_on_error=1:print_stacktrace=1
            tsan_options: ''
            test_timeout: '30'
            job_timeout: 30
            run_matrix: false
            check_target: check-sanitizer
            brew_pkgs: binutils
          - name: TSAN
            sanitizer: tsan
            extra_cflags: -O1 -g -fsanitize=thread -fno-omit-frame-pointer
            asan_options: ''
            ubsan_options: ''
            tsan_options: halt_on_error=1
            test_timeout: '60'
            job_timeout: 45
            run_matrix: false
            check_target: check-sanitizer
            brew_pkgs: binutils

    # contents: read for the checkout; pull-requests: read so the guard can
    # query the PR's current HEAD. (actions: write would let the guard
    # cancel the run instead of failing it, but repo policy caps the token
    # at actions: read, so the guard fails fast with a clear reason instead.)
    permissions:
      contents: read
      pull-requests: read

    concurrency:
      group: runtime-macos-${{ matrix.sanitizer }}-${{ github.ref }}
      cancel-in-progress: ${{ github.event_name == 'pull_request' }}

    env:
      LINUX_TOOLCHAIN: /opt/toolchain/aarch64-linux-gnu
      GNU_OBJCOPY: /opt/homebrew/opt/binutils/bin/objcopy
      EXTRA_CFLAGS: ${{ matrix.extra_cflags }}
      ASAN_OPTIONS: ${{ matrix.asan_options }}
      UBSAN_OPTIONS: ${{ matrix.ubsan_options }}
      TSAN_OPTIONS: ${{ matrix.tsan_options }}
      # Empty on the release leg leaves each script its own default: 60s in
      # tests/driver.sh, 10s in tests/lib/test-runner.sh. The 30 on the ASAN
      # and UBSAN legs is therefore a raise for the lanes and a cut for the
      # driver.
      TEST_TIMEOUT: ${{ matrix.test_timeout }}
      HOMEBREW_NO_INSTALL_CLEANUP: 1
      HOMEBREW_NO_AUTO_UPDATE: 1
      # qemu is only needed by test-matrix (release leg); sanitizer legs run the
      # fixture-free check-sanitizer subset and skip it.
      BREW_PKGS: ${{ matrix.brew_pkgs }}
      # Parallelize compilation; the guest-test cross-compile and elfuse build
      # dominate the non-test wall time.
      MAKEFLAGS: -j8

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      # Why this fails instead of cancelling: .ci/hvf-check-superseded.sh.
      - name: Fail fast if superseded by a newer PR commit
        if: github.event_name == 'pull_request'
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          RUN_SHA: ${{ github.event.pull_request.head.sha }}
        run: .ci/hvf-check-superseded.sh

      - name: Restore cached test fixtures
        # Only the release leg needs fixtures: the sanitizer legs run the
        # fixture-free check-sanitizer subset.
        if: ${{ matrix.run_matrix }}
        # actions/checkout's default clean:true runs `git clean -ffdx`, which
        # wipes externals/test-fixtures (gitignored) on this self-hosted
        # runner even though its disk otherwise persists across runs.
        # fetch-fixtures.sh is already idempotent -- it skips re-downloading
        # Alpine packages when externals/test-fixtures/versions.lock still
        # matches -- so stash that tree outside the workspace and restore it
        # here as a real directory. The qemu lane in tests/test-matrix.sh
        # shares the workspace root with the guest over virtio-9p, and a
        # symlink pointing outside that root does not resolve inside the
        # guest, so this must be a real copy, not a symlink.
        run: |
          cache="$HOME/.cache/elfuse-ci/test-fixtures"
          if [ -d "$cache" ]; then
            mkdir -p externals
            rm -rf externals/test-fixtures
            cp -Rc "$cache" externals/test-fixtures
            echo "Restored test fixtures ($(du -sh externals/test-fixtures | cut -f1), lock: $(head -1 externals/test-fixtures/versions.lock 2>/dev/null || echo none))"
          else
            echo "No fixtures cache at $cache; tests fetch on demand"
          fi

      - name: Host info
        run: |
          sw_vers
          uname -a
          uname -m
          sysctl kern.hv_support || true
          test "$(uname -m)" = "arm64"

      - name: Cache Homebrew downloads
        uses: actions/cache@v6
        with:
          path: ~/Library/Caches/Homebrew/downloads
          key: brew-runtime-${{ runner.os }}-${{ runner.arch }}-${{ env.BREW_PKGS }}

      - name: Install missing Homebrew packages
        run: |
          missing=()

          for pkg in $BREW_PKGS; do
            if ! brew list --formula "$pkg" >/dev/null 2>&1; then
              missing+=("$pkg")
            fi
          done

          if [ "${#missing[@]}" -gt 0 ]; then
            brew install --quiet "${missing[@]}"
          else
            echo "All Homebrew packages are already installed: $BREW_PKGS"
          fi

      - name: Tool versions
        run: |
          command -v make
          command -v "$GNU_OBJCOPY"
          make -V .MAKE.VERSION 2>/dev/null || true
          "$GNU_OBJCOPY" --version | head -1
          qemu-aarch64 --version | head -1 || true
          python3 --version

      - name: Check Rosetta for Linux
        # Rosetta is exercised only by test-matrix (release leg); the
        # check-sanitizer subset has no x86_64-via-Rosetta tests.
        if: ${{ matrix.run_matrix }}
        run: |
          ROSETTA=/Library/Apple/usr/libexec/oah/RosettaLinux/rosetta

          if [ ! -x "$ROSETTA" ]; then
            echo "::error::Rosetta for Linux runtime was not found at $ROSETTA"
            echo
            echo "Install Rosetta on the self-hosted Mac runner first:"
            echo "  sudo softwareupdate --install-rosetta --agree-to-license"
            echo
            echo "Current /Library/Apple/usr/libexec/oah contents:"
            ls -R /Library/Apple/usr/libexec/oah || true
            exit 1
          fi

          ls -l "$ROSETTA"

      - name: Build elfuse
        # make does not track EXTRA_CFLAGS changes, so an object built for one
        # sanitizer must not be reused for another. Checkout already wipes
        # build/ (git clean -ffdx), but clean explicitly so the leg builds from
        # scratch even on a workspace that was not freshly cleaned.
        run: |
          make clean
          make EXTRA_CFLAGS="$EXTRA_CFLAGS" elfuse

      - name: Verify HVF entitlement is embedded
        run: .ci/hvf-verify-elfuse-binary.sh build/elfuse

      - name: test-hello
        run: |
          make EXTRA_CFLAGS="$EXTRA_CFLAGS" test-hello

      - name: test-multi-vcpu
        run: |
          make EXTRA_CFLAGS="$EXTRA_CFLAGS" test-multi-vcpu

      - name: Set up Go
        # make check folds oci-test in only when the go on PATH meets
        # go.mod's floor, so the toolchain is installed first rather than
        # left to whatever the runner carries.
        if: ${{ matrix.run_matrix }}
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: make check
        # Release runs the full check suite; sanitizer legs run check-sanitizer,
        # a representative internal-implementation subset (the release lane plus
        # test-matrix already cover Linux syscall compatibility).
        run: |
          make EXTRA_CFLAGS="$EXTRA_CFLAGS" ${{ matrix.check_target }}

      - name: Test matrix
        if: ${{ matrix.run_matrix }}
        run: |
          bash tests/test-matrix.sh all

      - name: Build elfuse-oci
        # env: values do not expand $HOME, hence GITHUB_ENV. run-smoke and
        # exec-checks share the store.
        if: ${{ matrix.run_matrix }}
        run: |
          set -euo pipefail
          make elfuse-oci
          echo "OCI_STORE=$HOME/.cache/elfuse-ci/oci-store" >>"$GITHUB_ENV"

      - name: Reset OCI store
        # The store an earlier run left on this persistent disk is not
        # reused: a pin-table format change stranded it once, and clean
        # also detaches a bundle volume a cancelled run left attached.
        if: ${{ matrix.run_matrix }}
        run: build/elfuse-oci clean --store "$OCI_STORE"

      - name: OCI run smoke (pull -> sparsebundle -> COW clone -> HVF boot)
        # Release leg only: the sanitizer legs never boot a guest under
        # elfuse-oci.
        if: ${{ matrix.run_matrix }}
        run: |
          set -euo pipefail
          export ELFUSE_OCI_STORE="$OCI_STORE"
          scripts/ci/oci-run-smoke.sh

      - name: OCI execution checks (unix sockets, cold/warm boot, dynamic interp)
        # Release leg only; shares the smoke step's store, warm by then.
        if: ${{ matrix.run_matrix }}
        run: |
          set -euo pipefail
          export ELFUSE_OCI_STORE="$OCI_STORE"
          scripts/ci/oci-exec-checks.sh

      - name: Upload runtime binary
        if: ${{ !cancelled() }}
        uses: actions/upload-artifact@v7
        with:
          name: elfuse-runtime-${{ matrix.sanitizer }}-${{ runner.os }}-${{ runner.arch }}
          path: build/elfuse
          retention-days: 7
          if-no-files-found: warn

      - name: Save test fixtures cache
        # Persist externals/test-fixtures outside the workspace so the next
        # run's "Restore cached test fixtures" step can skip re-downloading
        # unchanged Alpine packages. Runs even if an earlier step failed, as
        # long as the job wasn't cancelled, so a fixture-unrelated test
        # failure doesn't cost the next run its cache.
        if: ${{ !cancelled() && matrix.sanitizer == 'release' }}
        run: |
          if [ -d externals/test-fixtures ]; then
            cache="$HOME/.cache/elfuse-ci/test-fixtures"
            mkdir -p "$(dirname "$cache")"
            rm -rf "$cache"
            cp -Rc externals/test-fixtures "$cache"
            echo "Saved test fixtures ($(du -sh "$cache" | cut -f1))"
          else
            echo "No externals/test-fixtures to save"
          fi

  # Per-image real workloads through elfuse-oci run under HVF. One leg
  # per image, fail-fast off: one image regressing must not hide the
  # state of the others. Each leg resets its per-key store first, so a
  # run never inherits state from the one before.
  workload:
    name: Workload (${{ matrix.key }})
    needs: build-macos
    if: >
      github.repository == 'sysprog21/elfuse' &&
      (github.event_name == 'push' || github.event_name == 'pull_request' ||
      github.event_name == 'workflow_dispatch' ||
      github.event_name == 'merge_group')
    runs-on: [self-hosted, macOS, arm64]
    timeout-minutes: ${{ matrix.timeout }}
    permissions:
      contents: read
      pull-requests: read
    concurrency:
      group: workload-${{ matrix.key }}-${{ github.ref }}
      cancel-in-progress: ${{ github.event_name == 'pull_request' }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { key: python, timeout: 30 }
          - { key: node, timeout: 30 }
          - { key: go, timeout: 30 }
          # jvm and c get 45: slower startup/compile bursts and the
          # largest first pulls.
          - { key: jvm, timeout: 45 }
          - { key: c, timeout: 45 }
          - { key: redis, timeout: 30 }
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: HVF elfuse setup
        uses: ./.github/actions/hvf-elfuse-setup
      - name: Reset OCI store
        # As on the release leg: no state from an earlier run.
        run: build/elfuse-oci clean --store "$HOME/.cache/elfuse-ci/oci-workload-${{ matrix.key }}"
      - name: Run ${{ matrix.key }} workload
        run: |
          set -euo pipefail
          export ELFUSE_OCI_STORE="$HOME/.cache/elfuse-ci/oci-workload-${{ matrix.key }}"
          scripts/ci/oci-workload.sh ${{ matrix.key }}
